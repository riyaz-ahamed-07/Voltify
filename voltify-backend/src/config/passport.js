// Google OAuth via Passport (optional - redirect flow)
// Currently using POST /api/auth/oauth/google with name+email approach
// If you want full Passport redirect flow, configure it here with:
// GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET in your .env

module.exports = {};
